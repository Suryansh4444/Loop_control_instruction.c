#include <stdio.h>

int main() {
    int count = 0; // Initialize counter

    // Loop to print "Happy Birthday" 1000 times using while loop
    while (count < 1000) {
        printf("Happy Birthday\n");
        count++; // Increment counter
    }

    return 0;
}
